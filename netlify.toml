# Netlify build for the Printly Next.js app.
# Data lives in Google Sheets via an Apps Script API (APPS_SCRIPT_URL + API_SECRET
# env vars). Files live in Netlify Blobs. The Next.js runtime is auto-detected.
[build]
  command = "next build"
